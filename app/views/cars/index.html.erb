<p id="notice"><%= notice %></p>

<h1>Cars</h1>

<table>
  <thead>
    <tr>
      <th>Make</th>
      <th>Model</th>
      <th>Vin</th>
      <th>Part</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.make.name %></td>
        <td><%= car.model %></td>
        <td><%= car.vin %></td>
            <%  l = car.parts.map {|p| p.name} %>
        <td><%= l.sort.join(", ") %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Car', new_car_path %>
<br>

<%= form_tag(search_cars_url, method: "get") do %>
  <%#= label_tag(:search, "Search by Model:") %>
<!--  <div class="">-->
    <%= text_field_tag :make, params[:make], placeholder: 'make',
                       data: { autocomplete: autocomplete_make_name_makes_path } %>
    <%= text_field_tag :model, params[:model], placeholder: 'model',
                       data: { autocomplete: autocomplete_car_model_cars_path } %>
    <%= text_field_tag :part, params[:part], placeholder: 'part',
                       data: { autocomplete: autocomplete_part_name_parts_path } %>
    <%= text_field_tag :vin, params[:vin], placeholder: 'vin' %>
<!--  </div>-->
  <%= submit_tag("search", class: 'positive ui button') %>
<% end %>
